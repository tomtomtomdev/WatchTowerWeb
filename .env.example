# Database connection
DATABASE_URL="postgresql://user:password@localhost:5432/watchtower?schema=public"

# RSA public key for apply-code login (single line, without BEGIN/END markers)
# Login credentials (email, password, base URL) are configured in Settings page
RSA_PUBLIC_KEY=MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8A...
